# Local Dev Control Center settings.
#
# Copy this file to config.sh or run ./start.sh --configure. Values already set
# in the environment take precedence over these local defaults.

export HOST="${HOST:-127.0.0.1}"
export PORT="${PORT:-3000}"
